---
/**
 * --------------------------------------------------------------------------- *
 *                     Copyright (c) by BookOfFretsX 2026                      *
 * --------------------------------------------------------------------------- *
 * @license https://github.com/TheAncientOwl/book-of-frets-x/blob/main/LICENSE
 *
 * @file SearchBar.astro
 * @author Alexandru Delegeanu
 * @version 1.0
 * @description Responsive search bar with a docked search icon.
 */

import { Icon } from 'astro-icon/components';

interface Props {
  placeholder?: string;
}

const { placeholder = 'Search items...' } = Astro.props;
---

<div class='relative w-full text-base'>
  <input
    type='text'
    data-search-input
    aria-label='Search items'
    placeholder={placeholder}
    class=`w-full rounded-lg py-2 pl-4 pr-10
        text-magenta bg-deep-purple-900 border border-magenta
        focus:outline-none focus:ring-2 focus:ring-magenta-700 focus:border-transparent
        placeholder-magenta-500 transition-all`
  />

  <div class=`absolute inset-y-0 right-0 flex items-center pr-3 pointer-events-none text-magenta`>
    <Icon name='tabler:search' class='w-5 h-5' />
  </div>
</div>

<script>
  const inputs = document.querySelectorAll('[data-search-input]');

  inputs.forEach(input => {
    input.addEventListener('input', event => {
      const target = event.target as HTMLInputElement;
      const searchEvent = new CustomEvent('search-change', {
        detail: { query: target.value },
        bubbles: true,
      });
      input.dispatchEvent(searchEvent);
    });
  });
</script>
